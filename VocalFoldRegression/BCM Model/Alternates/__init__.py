# Marker so the directory tracks in git. Female_GP.py and Female_TabPFN.py
# are non-transfer alternates on female BCM data, mirroring the
# Beam_Membrane/BM_*.py family. Cross-gender transfer comparator is the
# existing male->female RF in ResgressorAnalysis/figs/.
